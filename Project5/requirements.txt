# Core FastAPI dependencies
fastapi
uvicorn[standard]
pydantic

# Database dependencies
sqlalchemy

# Authentication dependencies (for Projects 3-5)
passlib[bcrypt]
python-jose[cryptography]
python-multipart

# Template and static file dependencies (for Project 5)
jinja2
aiofiles

# Testing dependencies
pytest
pytest-asyncio
httpx

# Database adapters (optional - only if using specific databases)
# psycopg2-binary  # PostgreSQL
# pymysql          # MySQL
